#include <stdio.h>

int main(){
    int size=20;
    char name[size];
    // scanf("%s",name);
    // or
    gets(name);


    // printf("Your name is %s",name);
    // or 
    puts(name);
    
    return 0;
}